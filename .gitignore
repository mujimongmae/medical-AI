# secrets & env
.env
.env.*
!.env.example

# personal Claude Code config (개인용, 공유 안 함)
.claude/settings.local.json

# medical data — 절대 커밋 금지
**/patient-data/
*.real.csv
*.phi.*

# node / next
node_modules/
.next/
out/
dist/
build/

# misc
.DS_Store
*.log
.vercel

# 빌드 산출물 apk
*.apk

# 브로커 로컬 저장(런타임 산출물, 합성이지만 커밋 안 함)
server/data.json

# Firebase 시크릿 (커밋 금지)
google-services.json
**/google-services.json
*firebase-adminsdk*.json
**/service-account*.json
firebase-service-account.json
