# 응급처치 프로토콜 · 트리아지 (일반인용) — CPR 판단 + AI 음성 추천

- **Status:** In Progress
- **Owner:** medical-domain / ai-engineer
- **Last updated:** 2026-07-02
- **관련 코드:** `lib/first-aid/schema.ts`, `lib/first-aid/protocols.ts`, `lib/first-aid/triage.ts`, `app/src/roles/NeighborView.tsx`, `server/claude.ts`
- **관련 스펙:** [`01-messaging-protocol.md`](./01-messaging-protocol.md) · [`../01-architecture/`](../01-architecture/README.md)

## 1. 목적 / 사용자 가치
홈캠 AI가 쓰러짐을 감지 → 이웃에게 자동 연락 → **비전문가 이웃이 따라 할 수 있게** 응급처치를 안내한다. 강원 급성심정지 생존율 7.2%(전국 최저)·일반인 CPR 시행률 도·군 19.9%의 "목격자 처치 공백"을 겨냥.
→ 근거: [`../../screening/03-ems-dispatch-problems.md`](../../screening/03-ems-dispatch-problems.md), [`../../screening/04-ems-bottleneck-deepdive.md`](../../screening/04-ems-bottleneck-deepdive.md)

## 2. 근거 가이드라인 (공신력 출처)
프로토콜 임상 내용의 출처. 각 프로토콜 객체는 `source[]`로 명시.
- **대한심폐소생협회(KACPR) 「2020 한국심폐소생술 가이드라인」** — 국내 1차 표준(일반인 가슴압박소생술)
- **AHA 2020 Guidelines for CPR & ECC**(2025 focused update) — 국제 교차참조
- **ILCOR CoSTR** · **소방청 119 DA-CPR** · **대한적십자사 응급처치법**
> ⚠️ 임상 수치는 가이드라인 개정 시 갱신. `source`에 판·연도 기록.

## 3. 동작 명세 (What) — 2갈래 흐름
진입: 쓰러짐 감지 → **119 자동 신고** → 가까운 이웃 3~4명 호출 → 이웃 도착.
그다음 **딱 하나만 먼저 가른다: "지금 CPR을 해야 하나?"**
- **(A) CPR 경로** — 의식·호흡이 **둘 다 없음** → 즉시 가슴압박 화면(시각자료 중심).
- **(B) AI 음성 추천 경로** — 의식이나 호흡이 **있음** → 이웃이 환자 상태를 **말로 설명** → STT → **Claude가 병력+증상 종합** → **가장 가능성 높은 상태 + 추천 응급처치**를 큰 글씨로 제시.

상태: 로딩 / 오프라인(**CPR 경로는 로컬 번들로 항상 동작**, AI 추천은 온라인일 때) / 완료(구급대 인계).

**UX 원칙(주 사용자 = 어르신):** 한 화면에 **한 메시지**. 작은 글자 많이 X → **큰 글씨 + 큰 시각자료(GIF·그림)**, 텍스트 최소. 세부 수치는 보조로 작게.

## 4. 트리아지 (진입 = 이웃 도착, 코드: `triage.ts`)
**단일 질문(두 단계로 나누지 않음, 어르신 UX·속도):**
```
Q. 환자가 반응이 있고, 숨도 정상적으로 쉬나요?
   ├─ 아니요 (반응이 없거나 / 숨을 안 쉼) ──▶ (A) CPR
   └─ 예 (반응도 있고 숨도 쉼) ────────────▶ (B) AI 음성 추천
```
- **CPR 조건 = 무의식 OR 무호흡 (하나라도 해당).** "둘 다 정상"일 때만 (B).
- 의심되면 CPR 쪽으로(주저하지 않기). 현장 안전 확인은 첫 화면 상시 안내.
- ⚠️ 임상 유의: "무의식+정상호흡"도 이 규칙에선 CPR로 감(회복자세 대신). 단순화·안전측 선택 — medical-domain 감수 필요(§9).

### 4.1 (A) CPR 화면 — 자동 진행
- 진입 즉시 **5초 카운트다운(5·4·3·2·1)** → "이 동작을 따라하세요" → **자동 시작**(이웃이 버튼 누르지 않음).
- **자동 사이클(반복):** 가슴압박 30회(분당 100~120, 박자음+진동+압박 애니메이션) → **"인공호흡 2회 하세요"를 잠깐 표시(약 5초, 호흡 리듬 안내)** → 사라지고 **다음 30회 자동 재개** → 계속 반복.
  - 인공호흡 단계도 압박처럼 **타이밍으로 안내**(문구+카운트) 후 자동 전환.
- **압박 위치 그림 + 큰 문구** 상시. 수동 조작은 **중지 버튼**만.
- 보조(작게): 성인 약 5cm / 중단 10초 이내 / AED·움직임·정상호흡·구급대까지 지속.
- GIF 대신 **CSS/SVG 자체 애니메이션**(오프라인·저작권 안전).

### 4.2 (B) AI 음성 추천 화면 — 자동 녹음
1. 진입 즉시 **마이크 권한 요청 + 자동 녹음 시작**(버튼 안 누름). "환자 상태를 말해 주세요" 큰 안내.
2. **STT**: 네이티브=Capacitor 음성인식 플러그인(RECORD_AUDIO 권한), 웹=Web Speech API, 미지원 시 텍스트 입력 폴백.
3. 멈춤(또는 종료) → 텍스트 → `POST /api/voice {transcript}` → 서버가 **환자 병력 + 증상**을 Claude에 전달 → 종합:
   - **가장 가능성 높은 상태 1개**("…가능성이 높아요") + **추천 처치**(짧게, 큰 글씨) + 매칭 프로토콜 id.
4. 예: "피가 많이 나요/얼굴이 파래졌어요/팔을 못 움직여요" → 지혈/질식/뇌졸중 처치 추천.
5. 하단 고정: **디스클레이머 + "119 지시 우선"**.

> **왜 AI인가**: 증상 조합·병력 반영은 고정 O/X로 다 못 담음. 자유 발화를 종합해 **가장 적절한 처치 하나**를 큰 글씨로.

## 5. 프로토콜 지식베이스 (8종, 코드: `protocols.ts`) — AI 추천의 근거 + 상세 화면
AI 추천은 아래 8종 지식베이스에 근거해 매핑한다. `P-CPR`/`P-AED`는 CPR 경로가 직접 사용.

| id | 이름 | 응급도 | 적용 |
|----|------|--------|------|
| `P-CPR` | 심폐소생술(가슴압박) | critical | 무의식+무호흡 → (A) 경로 |
| `P-AED` | 자동심장충격기(AED) | critical | 심정지 + AED 도착 |
| `P-CHOKING` | 기도폐쇄(질식) | critical | 이물질로 숨 못 쉼 |
| `P-STROKE` | 뇌졸중(FAST) | critical | 얼굴·팔·말 이상 |
| `P-BLEED` | 심한 출혈 지혈 | critical | 멈추지 않는 출혈·절단 |
| `P-SEIZURE` | 발작·경련 | urgent | 전신 경련 |
| `P-RECOVERY` | 회복자세 | urgent | 무의식+정상호흡 |
| `P-SYNCOPE` | 실신 | caution | 잠깐 정신 잃고 회복 |

각 프로토콜은 `steps`·`doNot`·`handoff`·`disclaimer`·`byPatient(성인/소아/영아)` 보유. (상세 임상 내용은 코드가 진실 원천)

## 6. 로직 / 규칙 (How) — 안전 원칙 (개정)
- **생명 직결 결정(CPR 여부)은 규칙 기반**: 의식·호흡 2개로만 판단, **LLM에 위임 안 함**. 헐떡임=심정지로 간주, 애매하면 시행. 일반인 기본 = 가슴압박(hands-only).
- **그 외 상태 판단·처치 추천은 AI 보조**(경로 B): 단
  - **확정 진단 금지** → "…일 가능성이 높아요" 식 표현.
  - **119/구급대 지시 우선**, 환자 **병력 반영**, **디스클레이머** 고정 노출.
  - AI는 지식베이스(8종) 범위 내에서 추천, 위험/불명확하면 "구급상황실(119)에 물어보세요"로 안내.
- **대상 구분**(성인/소아/영아) 수치는 프로토콜 `byPatient`로.
- **어르신 UX**: 한 화면 한 메시지, 큰 글씨·큰 시각자료, 음성 우선.

## 7. 데이터 / 스키마 (코드)
- `schema.ts`: `FirstAidProtocol`,`ProtocolStep`,`TriageNode`,`GuidelineRef`,`Urgency`,`PatientType`,`RepeatRule` + `GLOBAL_DISCLAIMER`.
- `triage.ts`: §4 결정트리(의식→호흡→CPR/AI).
- `protocols.ts`: 8종 지식베이스.
- `server/claude.ts`: (B) 경로의 병력+증상 종합·추천 로직(구조화 출력).
- PHI/PII 없음(합성만). CPR 경로는 **오프라인 번들** 가능해야 함.

## 8. 아키텍처 연동
- (A) CPR: 전부 클라이언트 로컬(오프라인 OK) + 메트로놈.
- (B) AI: 음성→STT(온디바이스)→`POST /api/voice`→Claude→추천 반환→큰 글씨 표시. (spec 01 §2.2)
- 환자 병력은 등록 시 입력, 서버가 이벤트의 환자 병력을 Claude 컨텍스트로 사용.

## 9. 의료 안전성 체크
- [x] 디스클레이머(`GLOBAL_DISCLAIMER` 상단 + 프로토콜별 `disclaimer`)
- [x] 응급 → **119 최우선**(자동신고 전제, CPR 화면·AI 화면 모두 명시)
- [x] **확정 진단/처방 금지** — CPR은 상태 기반 행동, AI는 "가능성" 표현 + 구급대 지시 우선
- [x] 대상(성인/소아/영아) 구분
- [ ] **medical-domain 임상 정확성 감수**(CPR 문구·AI 추천 프롬프트 포함) — 구현 후 필수
- [ ] AI 추천의 안전 가드 검증(위험 추천 억제, 불명확 시 119 안내)

## 10. 미해결 / TODO
- [ ] **CPR GIF·압박 위치 그림 에셋** 확보(실물 GIF 또는 자체 애니메이션 대체).
- [ ] `/api/voice` 응답을 추천 구조로 확장(요약+가능상태+추천+protocolId) → spec 01 반영.
- [ ] triage.ts를 새 흐름(의식→호흡→CPR/AI)으로 갱신.
- [ ] AI 추천 프롬프트 설계(병력+증상→가장 가능성 높은 상태 1개, 확정진단 금지).
- [ ] 홈캠 감지 ↔ 트리아지 진입 정합.
- [ ] medical-domain 임상 감수.
